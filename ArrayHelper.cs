using System;

namespace Program
{
    public class ArrayHelper<T>
    {
        public static T Pop<T>(ref T[] arr)
        {
            int endElement = arr.Length;
            T results = arr[endElement];
            return T;
        }
    }
}